# 弋阳县充换电设施规划系统 - 动态架构

## 架构说明

采用前后端分离的动态架构：

```
┌─────────────┐      HTTP/JSON      ┌──────────────┐
│   浏览器     │ ←─────────────────→ │  Flask 后端   │
│  (前端)      │    RESTful API      │   (app.py)   │
└─────────────┘                      └──────────────┘
                                            ↓
                                     ┌──────────────┐
                                     │   SQLite DB  │
                                     │ yiyang_ev.db │
                                     └──────────────┘
```

## 启动方式

```bash
cd "C:\Users\lhn\OneDrive\Desktop\江西\弋阳充电桩项目"
python app.py
```

访问：http://localhost:5000

## API 端点

| 端点 | 说明 | 返回数据 |
|------|------|----------|
| `GET /` | 主页面 | HTML |
| `GET /api/config` | 地图配置 | 天地图Key、中心点、样式配置 |
| `GET /api/boundary` | 县域边界 | 111个坐标点 |
| `GET /api/stations/urban` | 现有充电站 | 16座，含精确坐标 |
| `GET /api/stations/gas` | 加油站 | 26座 |
| `GET /api/stations/planned` | 规划充电站 | 34站 |
| `GET /api/stats` | 统计摘要 | 汇总数据 |
| `GET /api/economic` | 经济数据 | 2016-2025年时间序列 |

## 前端功能

- **动态数据加载**：页面启动时通过 Fetch API 并行加载所有数据
- **天地图集成**：动态注入天地图脚本，支持标准/卫星两种底图
- **县域聚光灯**：使用真实边界数据（111点），周边区域半透明遮罩
- **三层数据**：现有充电站（绿）、加油站（橙）、规划站（蓝）
- **5种显示模式**：全部、仅边界、现状分析、规划视图、加油站分布
- **图层控制**：独立开关每个图层
- **交互弹窗**：点击标注查看详细信息

## 文件结构

```
弋阳充电桩项目/
├── app.py                  # Flask 后端主程序
├── config.py               # 配置中心
├── data_loader.py          # 数据加载模块
├── frontend/
│   └── index.html          # 前端单页应用（含CSS/JS）
├── data/
│   └── yiyang_boundary.json  # 县域边界坐标
└── db/
    └── yiyang_ev.db        # SQLite 数据库
```

## 优势

1. **前后端分离**：便于维护和扩展
2. **数据实时性**：修改数据库后刷新页面即可看到更新
3. **API 可复用**：其他应用可直接调用 API
4. **易于部署**：可部署到云服务器供多人访问
5. **可扩展性**：易于添加新功能（搜索、筛选、统计图表等）

## 下一步扩展方向

- [ ] 添加搜索功能（按站点名称、乡镇搜索）
- [ ] 数据筛选（按年份、功率、场景类型）
- [ ] 统计图表（ECharts 集成）
- [ ] 数据导出（Excel/PDF）
- [ ] 用户认证（管理员编辑数据）
- [ ] 生产部署（Gunicorn + Nginx）

## 技术栈

- **后端**：Flask 3.1.3 + Flask-CORS 6.0.2
- **数据库**：SQLite 3
- **前端**：原生 HTML/CSS/JavaScript（无框架依赖）
- **地图**：天地图 API v4.0
- **数据处理**：Pandas 3.0.1

## 开发模式

Flask 运行在 debug 模式，代码修改后自动重启。生产环境需要：

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

配合 Nginx 反向代理使用。
